```diff
diff --git a/packages/svelte2tsx/src/svelte2tsx/utils/civetUtils.ts b/packages/svelte2tsx/src/svelte2tsx/utils/civetUtils.ts
index 8b28b6b..c01a3d5 100644
--- a/packages/svelte2tsx/src/svelte2tsx/utils/civetUtils.ts
+++ b/packages/svelte2tsx/src/svelte2tsx/utils/civetUtils.ts
@@ -54,5 +54,15 @@
   const dedentedLines = lines.map(line =>
     line.startsWith(indent) ? line.slice(indent.length) : line
   );
-  return { dedented: dedentedLines.join('\n'), indent };
+  let finalDedentedCode = dedentedLines.join('\n');
+
+  // If the first line of the input 'snippet' (which is 'lines[0]') consisted *only* of the common indent,
+  // and this resulted in an empty first line in 'dedentedLines' (now represented as a leading '\n' in 'finalDedentedCode' if >1 lines),
+  // and there's actual content on subsequent lines (or it was the only line and became empty),
+  // then this leading newline is an artifact of dedenting an effectively blank formatting line.
+  // We remove this artifactual leading newline to ensure the civetMap.source starts with actual content if present.
+  if (lines.length > 0 && lines[0] === indent && finalDedentedCode.startsWith('\n')) {
+    finalDedentedCode = finalDedentedCode.substring(1);
+  }
+  return { dedented: finalDedentedCode, indent };
 } 
diff --git a/packages/svelte2tsx/test/civet/9 - LazerFocusBlankLine.test.ts b/packages/svelte2tsx/test/civet/9 - LazerFocusBlankLine.test.ts
index a255849..c677731 100644
--- a/packages/svelte2tsx/test/civet/9 - LazerFocusBlankLine.test.ts
+++ b/packages/svelte2tsx/test/civet/9 - LazerFocusBlankLine.test.ts
@@ -7,7 +7,7 @@
 describe('9 - LazerFocusBlankLine: mapping with leading blank lines #current', () => {
   const fixtures = [
     { name: 'LazerFocus2-issue4-allgood.svelte', expectOffset: 0 },
-    { name: 'LazerFocus2-issue4.svelte', expectOffset: 0 /* expect broken offset */ }
+    { name: 'LazerFocus2-issue4.svelte', expectOffset: 0 /* expect zero offset after fix */ }
   ];
 
   for (const { name, expectOffset } of fixtures) {
@@ -34,11 +34,7 @@
 
       const offset = pos.line - usageLine1;
       console.log(`${name} offset: ${offset}`);
-      if (name === 'LazerFocus2-issue4-allgood.svelte') {
-        assert.strictEqual(offset, expectOffset, `Expected offset ${expectOffset} for ${name}, got ${offset}`);
-      } else {
-        assert.notStrictEqual(offset, 0, `Offset for ${name} should be non-zero (found ${offset})`);
-      }
+      assert.strictEqual(offset, expectOffset, `Expected offset ${expectOffset} for ${name}, got ${offset}`);
     });
   }
 }); 
```