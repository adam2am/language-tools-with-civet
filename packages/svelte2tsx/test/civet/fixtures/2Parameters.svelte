<script lang="civet">
	great .= $state(1)
	value .= $state(1)
	propsProbl := (ab: number, bc: number) =>
		value = ab * bc
	
	propsGreat1 := (gf: number, h: number) =>
		value = gf * h

	propsGreat2 := (de: number, ef: number) =>
		great = de * ef
		ghk := 123 
</script>
<div>{fruits}</div> 